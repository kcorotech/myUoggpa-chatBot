name: Update Chatbot Data

on:
  workflow_dispatch: # This adds a manual "Run" button in GitHub

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
          
      - name: Run Data Builder
        run: python build_data.py
        
      - name: Commit and Push changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add public/uogData.json public/version.json
          git commit -m "🤖 Auto-updated chatbot data from Google Sheets" || echo "No changes to commit"
          git push